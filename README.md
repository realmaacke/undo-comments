## undo-comments

This package is for undo comments in json, since JSON.parse dosent support comments.


### Usage:

```
npm install undo-comments
```
```
import { undoComment } from 'undo-comments';
```